# LogScope 方案设计

> **LogScope**（产品目录 `log_analysis`）—— 日志问题定位 agent。
> 输入：**长日志 + 相关代码库**；输出：**定位到哪一行代码**有问题（不判「谁的」——客户/我方代码都丢进来，不做责任归属分类）。
> 状态：**v1 成品已建**（4 agents + `/diag` + 配置/文档）；待真实长日志验证。升级路径：Drain3 / MCP log server。

---

## 0. 设计基线

- **借鉴方法不借代码**：复用社区验证过的好模式（见 §11 借鉴清单），本仓重写。
- **成品优先**：CRG（code-review-graph，代码图）+ DCP（opencode-dcp，上下文兜底）用现成成品；日志结构化用纯 Python 脚本（Drain3，`uv run --with drain3`），跨平台不依赖 Unix 工具/Git Bash。
- **日志是数据源，不是上下文**：原始日志不进 LLM 上下文；先分流成有界 digest，按需回读行段。
- **只定位，不分类**：目标是「定位到代码行」，不做责任归属（我方缺陷/客户误用/报错≠现象/环境/已知那一层全部不要）。代码不管谁的都丢进来分析。
- **本地/隐私**：LLM 走公司内网 glm-5.2；代码不外发；CRG 本地 SQLite；日志可能含客户数据，分析在本机/内网。
- **人在 loop**：报告 checkpoint 交人审；不自动改代码、不自动 commit。

---

## 1. 核心思想：日志数据源 + 双层索引

| 层 | 角色 | 特性 | 加载 |
|---|---|---|---|
| 日志 digest | 信号层：错误信息 / 栈帧 / 时间线 / 关键符号 | 小、有界 | triage 产，agent 先读 |
| 代码库（图+源+wiki） | 真相层：how / 调用链 / 契约 | 大 | 经 digest 里的符号锚点 + CRG 图，**引导选择性加载** |

**主线**：triage 把长日志压成 digest → 用 digest 里的符号让 code-tracer 沿代码图反向回溯 → **定位到 `file:line`**。

---

## 2. 三层上下文管理（长日志防爆）

| 层 | 方案 | 落地 |
|---|---|---|
| **入口层** | 预处理/结构化 | `log-triage` 截断/提取/模板归并（借 Drain3 思路，~28:1 压缩）→ **有界 digest**；原始日志写临时文件，返回预览指针 |
| **核心层** | 动态控制 | opencode `compaction.auto+tail_turns` + DCP 兜底；`logscope-dev` 自校正 Critic（证据链复核、有界迭代） |
| **兜底层** | 外部存储+按需检索 | v1：`log-triage` 的预览指针 + opencode `read` 工具回读行段。升级：MCP log server（search_logs/correlate/trace_ids） |
| **分层记忆** | 短期窗口 + 锚点 | 短期=最近几轮；锚点=错误点/定位假设；长期=历史日志向量库（未来） |

**关键标识符保留**：压缩时保 UUID/文件路径/行号/时间戳/error code（DCP `protectTags` 或 prompt 硬约束）。

---

## 3. 工作流（`/diag`）

入：日志（文件路径 or 文本）、`<repo>`、可选 `<wiki>`。

1. **log-triage**：长日志 → 有界 digest（错误 + 栈帧 + 时间线 + 关键符号，Drain3 模板归并）。原始日志写 `~/.logscope/tmp/log_<ts>.txt` + 返回预览指针（方案三：找优于记）。
2. **wiki-reader**（索引优先）：有业务流 wiki → 先读小 `error_index.md` 用 digest 错误信号匹配 → 命中直接拿 `throw_file:line`（连页都不读）或按需读单页取调用链；无 wiki/未命中退回源码。**不全量灌 wiki**（见 §3.1）。
3. **code-tracer**：CRG 新鲜度门（问用户 build/update/不跑）→ 定位 throw 点 → `query callers_of` 反向回溯 → `impact`+`flow` 找偏离点 → **定位 `file:line`**。
4. **logscope-dev 汇总 + 自校正**（借 Sherlog Critic）：串证据链，弱则带反馈回 code-tracer 重试（≤3 次）。**错误链追源**（借 Sherlogs）：日志里每条错分「originating（本处抛）/relaying（转发别处不可达）」→ 沿 relaying 链追到 origin。
5. **报告**：定位结论（哪行代码 + 置信度 + 证据链 log行号+file:line+图边+契约段）→ 🛑人审。

> 没有责任分类步骤——定位到行即交付。

### 3.1 wiki 高效检索（索引式，不灌上下文）

`/wiki-flow` 产两类：各生命周期页（全，留盘）+ `error_index.md`（聚合错误目录，小）。`/diag` 的 wiki-reader 只读 index 匹配 → 命中条目直接给 `throw_file:line`（常连页都不读）；需调用链再按需读单页相关段。**索引入上下文，全页留盘按需取**——和「日志是数据源不当上下文」同构。

---

## 4. Agent 拓扑 + 权限

primary `logscope-dev` 编排 + 人在 loop；其余 subagent，最小权限（read-only 的 bash = `*`:deny, `git *`:allow, `code-review-graph *`:allow）。

| Agent | mode | 职责 | 关键权限 |
|---|---|---|---|
| `logscope-dev` | primary（默认） | 编排 /diag、汇总+自校正复核、报告 checkpoint | edit: allow, bash: allow（禁 commit/push） |
| `log-triage` | subagent | 长日志→有界 digest（Drain3 模板簇+hisysevent+faultlog）+ 预览指针 | edit: deny, bash: `uv *`+`git *`（不依赖 Unix 工具） |
| `code-tracer` | subagent | CRG 门 + search/query/impact/flow 回溯定位 file:line（只读） | edit: deny, bash: 只读 git + CRG |
| `wiki-reader` | subagent | 读 wiki 契约 + 源码头（只读） | edit: deny, bash: 只读 git + CRG |

> v1 不单独设 `log-indexer`——log-triage 已做有界 digest + 预览指针；超大日志的语义检索升级由 MCP log server 取代（见 §5 升级路径）。

---

## 5. 日志处理流水线（log-triage）

**log-triage**（入口层，必跑，纯 Python 跨平台）：
- 输入：日志文件路径 or 文本。
- 落盘（若文本）：opencode `write` 工具写 `~/.logscope/tmp/log_<ts>.txt`（home，跨平台；文件路径直接用）。原始日志**不进上下文**。
- 结构化：`uv run --with drain3 python ~/.logscope/scripts/drain3_triage.py <raw> --top 50` —— 纯 Python 一体产出 Drain3 模板簇 + HiSysEvent 事件（`FILE/LINE/CALLER` 锚点）+ faultlog 栈帧 + 本次新见簇。模板持久化 `~/.logscope/templates/<profile>.json` 跨 run 累积。
- 有界 digest：脚本输出即 digest（内部截断）；标出 **claimed error**。
- 取证行段：opencode `read` 工具（offset/limit）回读，不用 bash `sed`。

**跨平台**：只用 `uv`+`python`+`git`+opencode 原生工具，不依赖 Unix 工具（grep/sed/awk）或 Git Bash，Windows 原生 opencode (PowerShell) 可跑。

**升级路径**：
- ✅ **Drain3 已装 + 持久化**（替 sed 归并；跨 run 累积，新见簇标信号）。改源脚本后重装：`cp log_analysis/scripts/drain3_triage.py ~/.logscope/scripts/`。
- ⏳ 接 **MCP log server**（首选 `wolven-tech/mcp-log-server`；语义检索 `frannuca/mcplogger`，OpenAI 兼容端点可指内网 glm）把结构化 MCP 工具接入。

---

## 6. 代码回溯（code-tracer）

- **CRG 新鲜度门**：`status --json`+`detect-changes --brief` → 缺失/过时则 `question` 问用户 build/update/不跑（不擅自建图）。
- **定位 throw 点**：用 opencode `grep` 工具搜错误 message 串 → 找到 throw/file:line。
- **反向回溯**：`code-review-graph search <符号> --repo <R>` → `query callers_of <target>` 反向上溯 → 判断 throw 是不是**下游症状**（上游条件触发→错位抛错/catch 换 message），找真正偏离点。
- **blast radius + flow**：`impact --files <症状文件>` + `flow --name <入口> --source` → 沿调用链找偏离步。
- `visualize --format json` 仅兜底（重）。
- read 工具读源码取证，定位到 `file:line`。

---

## 7. 成品共用与配置独立

- **共用成品**：CRG、DCP、glm-5.2 内网端点——本仓各自配置（LogScope 自己的 `opencode.json`）。
- **wiki-reader**：读目标仓自带的 wiki（任何来源，有则用，无则源码头注释）。

---

## 8. 配置（`log_analysis/opencode.json`，已写）

- `$schema`、`default_agent: "logscope-dev"`、`instructions: ["AGENTS.md"]`
- `permission`：edit: ask；bash `*:ask, git *:allow, code-review-graph *:allow, git commit/push:deny, rm *:deny`；external_directory: allow
- `compaction: {auto:true, tail_turns:15}`（+ DCP 全局插件兜底）
- model 由全局提供（glm-5.2）；DCP 阈值全局已调 75%/45%

---

## 9. 目录结构

```
/home/dlrow_hl/log_analysis/        # 产品仓（LogScope）
  .opencode/agents/   # logscope-dev, log-triage, code-tracer, wiki-reader
  .opencode/commands/ # /diag
  opencode.json       # 配置（default_agent=logscope-dev、权限、compaction）
  AGENTS.md            # 产品 agent-facing
  README.md            # 用户使用说明
  方案设计.md          # ← 本文件
  完成情况.md          # 开发/验证状态
  .gitignore
/home/dlrow_hl/log_analysis_dev/   # 开发工作区
  AGENTS.md            # 开发约定（指向 log_analysis）
  opencode.json
```

---

## 10. 落地状态 + 待办

- ✅ 目录：`log_analysis/` + `log_analysis_dev/` 已建
- ✅ agents：logscope-dev / log-triage / code-tracer / wiki-reader（4 个）
- ✅ commands：/diag
- ✅ `opencode.json` + `AGENTS.md` + `README.md` + `完成情况.md` + `.gitignore`
- ✅ `log_analysis_dev/AGENTS.md` + `opencode.json`（开发工作区）
- ⏳ 用真实长日志 + 代码仓验证 /diag 端到端
- ⏳ 升级：装 Drain3 / 接 MCP log server

---

## 11. 调研借鉴清单（独立项目，借方法不借代码）

- **Drain3**（`logpai/Drain3`，788★）：在线日志模板挖掘；v1 用 sed 实现其思路，升级可装（精度更高+参数抽取+PII masking）。
- **Sherlog**（`kqiu10/Sherlog`）：失败日志→RAG 历史 incident→诊断→提修复→**Critic 自校正循环**（有界迭代）。关键证据：tool-augmented（读代码 via MCP）100% vs 纯日志 83%。借：自校正 Critic 环。
- **Sherlogs**（`rohitraju1966/Sherlogs`）：Drain3→DuckDB 两表→LangGraph agent，92% AC@1；有界工具输出 + hop 预算 + **错误链追源**（originating 抛错 vs relaying 转发→追到 origin）。借：错误链追源 + 有界工具。
- **mcp-log-server**（`wolven-tech/mcp-log-server`）：12 工具 token-efficient 日志 MCP（search_logs/correlate/trace_ids/summarize），自动检测 JSON/纯文本、severity 过滤、持久索引、流式。升级路径用它替 grep。
- **NVIDIA log_analysis_multi_agent_rag**：BM25+FAISS+rerank+grade+self-correct 参考。
- **pylogtracer**（`KarthikKashi98`）：pip 即用、provider 无关、ReAct agent、巨文件 bounded/tail——备选库。

---

## 12. 备注 / 后续

- 日志格式多样（HiSysEvent/logcat/服务端日志）：log-triage 的 grep 模式可配 `--log-format`，或 agent 自适应。
- 历史日志长期向量库（方案三长期）：未来接内网向量库，跨 case 检索「这错踩过没」给定位加先验。
- 可观测后端 MCP（`otel-mcp-server`/`lumino`/`logintel`）：日志在 Datadog/Loki/CloudWatch 而非文件时，未来接。
- 与「公司内网搜索辅助 Debug」协同：内网搜已知问题/错误模式辅助定位。
