# LogScope 完成情况

> 开发与验证状态总览。**每次开发或验证后同步更新本文件**（不进 `.gitignore`，纳入版本管理）。
> 最近更新：2026-07-23

## 成品接线

| 项 | 状态 | 说明 |
|---|---|---|
| CRG（code-review-graph） | ✅ 已装 | `~/.local/bin/code-review-graph`，`--repo` 任意路径 |
| DCP（opencode-dcp） | ✅ 借用（全局已装，阈值 75%/45%） | 上下文兜底 |
| 日志结构化 v1 | ✅ bash grep+sed 模板归并（借 Drain3 思路） | log-triage 内置 |
| Drain3（升级） | ✅ 已装+持久化 | `uv run --with drain3`，`scripts/drain3_triage.py`；模板存 `templates/<profile>.json` 跨 run 累积，本次新见簇标为信号（实测 run1 全新 31 簇→run2 0 新簇） |
| MCP log server（升级） | ⏳ 未接 | 首选 `wolven-tech/mcp-log-server` |

## .opencode/ 配置

| 项 | 状态 | 说明 |
|---|---|---|
| 5 agents（logscope-dev / log-triage / code-tracer / wiki-reader / flow-writer） | ✅ 已写 | 见 `.opencode/agents/` |
| 2 commands（/diag /wiki-flow） | ✅ 已写 | 见 `.opencode/commands/` |
| `opencode.json` + `AGENTS.md` + `README.md` + `.gitignore` | ✅ 已写 | |

## 工作流

| 流 | 命令 | 开发 | 验证 | 说明 |
|---|---|---|---|---|
| 日志问题定位 | `/diag` | ✅ | ✅ 鸿蒙样本 | harmony profile（hilog/HiSysEvent/faultlog）已加 + 用 `test/sample_harmony_avsession.log` 端到端验证：digest 抽出 FILE/LINE 锚点 → code-tracer 定位 `avsession_radar.cpp:248` ✓ |
| 业务流 wiki 生成 | `/wiki-flow` | ✅ | ✅ avsession 投播样本 | flow-writer agent + `test/avsession_cast_flow.md`（调用链+错误目录→`avsession_radar.cpp:248`）+ `error_index.md` 索引式检索（wiki-reader 只读 index 不灌 wiki）|

## 鸿蒙日志 profile（`--log-format harmony`）

- ✅ **hilog parser**：`07-24 10:00:02.310 1200 1205 E A0D04/AVSession: msg` — 抽 datetime/pid/tid/level/domainID/tag/msg，过滤 E/F。
- ✅ **HiSysEvent parser**：JSON 行，抽 domain/name/type(FAULT)/level/params（FILE/LINE/CALLER/REASON 是金锚点）。
- ✅ **faultlog parser**：native 栈 `#NN pc <裸hex> /path/lib.so(buildId)` + ArkTS 栈 `at func (path:line:col)` + fault 头（Reason/signal）。
- ✅ **模板归并**：hilog 变量替换+合并（实测 3 次重复 E/F 合并）。
- ✅ **domain→模块**：tag/domain 列在 digest，code-tracer 用 tag 当符号 grep 代码仓。

## 文档

| 文件 | 状态 | 说明 |
|---|---|---|
| `方案设计.md` | ✅ 同步 | 设计稿（拓扑/三层 context/工作流/成品关系/升级路径） |
| `README.md` | ✅ 同步 | 用户使用说明 |
| `AGENTS.md` | ✅ 同步 | 产品 agent-facing |
| `完成情况.md` | ✅ 新建 | 本文件 |

## 借鉴清单（独立项目，借方法不借代码）

- **Drain3**（logpai/Drain3）：日志模板挖掘思路（v1 用 sed 实现，升级可装）。
- **Sherlog**（kqiu10）：自校正 Critic 环（证据链复核、有界迭代）。
- **Sherlogs**（rohitraju1966）：Drain3→DuckDB 管线、有界工具输出、错误链追源（originating vs relaying）。
- **mcp-log-server**（wolven-tech）：结构化日志 MCP 工具（search_logs/correlate/trace_ids），升级路径。

## 环境备注

- 生产：Windows + Git Bash（opencode shell 指向 Git Bash）；当前开发：Linux（WSL）。命令一致。
- 独立项目；共用成品（CRG/DCP/glm-5.2）但各自配置。
- 开发工作区：`/home/dlrow_hl/log_analysis_dev`。

## 待办

- [x] 鸿蒙日志 profile（hilog/HiSysEvent/faultlog）加进 log-triage.md + 样本日志端到端验证（`test/sample_harmony_avsession.log`）
- [x] 业务流 wiki（flow-writer + /wiki-flow + avsession 投播样本页 `test/avsession_cast_flow.md` + `error_index.md` 索引式检索：wiki-reader 只读 index 不灌 wiki）
- [ ] 用真实客户鸿蒙长日志（hilog+hisysevent+faultlog 混合）跑 /diag，验证 digest 质量 + 大仓定位准度
- [ ] 装 Drain3 升级 log-triage 的结构化（替 sed 归并）
- [ ] 接 `wolven-tech/mcp-log-server` 把 log-triage 的 grep 换成结构化 MCP 工具
- [ ] native 栈 .so 符号化（`llvm-addr2line`/`hstack` + app .so/debug .so + sourceMaps.map）——目前 app 库栈标「待符号化」，ArkTS/hisysevent 锚点可直接定位
- [ ] Windows + Git Bash 生产实跑验证
- [ ] 接公司内网搜索辅助定位（未来，见 方案设计 §12）
